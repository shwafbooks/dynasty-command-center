// Isolated Team Intelligence UI. Reads verified DCC data only.
(()=>{const POSITIONS=['QB','RB','WR','TE'];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let report=null,evidence=null,throughWeek=null;
function positionCard(row){const players=(row?.players||[]).filter(p=>p.hasVerifiedScoring).slice(0,3),cmp=row?.comparison;return`<article class="ti-position-card"><div class="ti-position-card__top"><span>${esc(row?.position||'—')}</span><strong>${Number(row?.seasonPoints||0).toFixed(1)}</strong></div>${cmp?`<div class="ti-rank"><b>#${cmp.rank} of ${cmp.leagueSize}</b><span>${cmp.pointsVsLeagueAverage>=0?'+':''}${Number(cmp.pointsVsLeagueAverage).toFixed(1)} vs avg</span></div>`:''}<small>verified roster pts · ${Number(row?.scoringPlayers||0)}/${Number(row?.rosterCount||0)} scoring</small><div class="ti-position-card__players">${players.length?players.map(p=>`<span>${esc(p.name)} <b>${Number(p.seasonPoints||0).toFixed(1)}</b></span>`).join(''):'<span>Insufficient verified production</span>'}</div></article>`;}
function evidenceCard(label,value,unit,status){const measured=value!==null&&value!==undefined&&Number.isFinite(Number(value));return `<article class="ti-evidence-card"><span>${esc(label)}</span><strong>${measured?esc(Number(value).toFixed(unit==='pts'?1:3)):'—'}</strong><small>${measured?esc(unit):'Insufficient data'} · ${esc(status)}</small></article>`;}
function evidencePanel(team){const row=(evidence?.strengthAxes?.teams||[]).find(t=>String(t.rosterId)===String(team.rosterId));if(!row)return '<section class="ti-evidence"><h4>Competitive & dynasty evidence</h4><p class="ti-empty">Evidence is unavailable. No values have been estimated.</p></section>';const c=row.competitiveEvidence||{},d=row.dynastyAssetEvidence||{};return `<section class="ti-evidence"><div class="ti-heading"><div><span class="team-accordion__eyebrow">TWO DISTINCT EVIDENCE AXES</span><h4>Competitive & dynasty evidence</h4></div><small>Measured components · no overall ranking</small></div><div class="ti-evidence-grid"><div class="ti-evidence-group"><h5>Current competitive evidence</h5><p>Verified current-roster fantasy production, not historical team points-for.</p>${evidenceCard('Verified production',c.verifiedProduction,'pts',c.status||'unavailable')}</div><div class="ti-evidence-group"><h5>Dynasty asset evidence</h5><p>Independent market consensus for the optimized lineup and owned depth; replacement is context only.</p>${evidenceCard('Lineup market core',d.lineupMarketCore,'consensus percentile',d.status||'unavailable')}${evidenceCard('Owned depth',d.ownedDepth,'consensus percentile',d.status||'unavailable')}${evidenceCard('Replacement context',d.replacementContext,'market advantage',d.status||'unavailable')}</div></div><details class="ti-evidence-audit"><summary>Sources, formula & limitations</summary><p>Production: verified Sleeper player scoring under league rules. Dynasty market: consensus from at least two independent evidence groups; lineup optimized to league slots. Values are descriptive, not projections. Replacement context is not added to owned depth because of correlated evidence. Missing inputs are withheld, not zero. No AI adjustments, cross-axis weights, overall score or overall ranking.</p><small>${esc(evidence?.provenance?.formulaVersion||'component matrix')} · ${esc(evidence?.strengthAxes?.provenance?.formulaVersion||'strength axes')} · through Week ${esc(throughWeek)}</small></details></section>`;}
function panel(team){const intelligence=(report?.teams||[]).find(row=>String(row.rosterId)===String(team.rosterId)),section=document.createElement('section');section.className='team-intelligence-panel';if(!intelligence){section.innerHTML='<div class="ti-heading"><div><span class="team-accordion__eyebrow">TEAM INTELLIGENCE</span><h4>Verified Production</h4></div><small>Insufficient data</small></div><p class="ti-empty">DCC will publish this profile only when verified scoring is available.</p>';section.insertAdjacentHTML('beforeend',evidencePanel(team));return section;}section.innerHTML=`<div class="ti-heading"><div><span class="team-accordion__eyebrow">TEAM INTELLIGENCE · V1.1</span><h4>Verified Positional Production</h4></div><small>Through Week ${esc(throughWeek)} · ${esc(report.formulaVersion||'deterministic')}</small></div><div class="ti-position-grid">${POSITIONS.map(position=>positionCard(intelligence.positions?.[position])).join('')}</div><div class="ti-audit"><strong>${Number(intelligence.totalRosterPoints||0).toFixed(1)} verified roster points</strong><span>Current-roster production · descriptive, not a projection</span><details><summary>Audit formula</summary><p>DCC sums verified fantasy points for players currently on this roster and groups them by listed position. Position rank is the descending positional total across all 10 franchises; exact ties use roster ID only for display order. “Vs avg” is this team's positional total minus the 10-team league average. This is not dynasty value, historical points-for, or a future-strength prediction. No AI, age, projection, or market-value adjustment is used.</p></details></div>`;section.insertAdjacentHTML('beforeend',evidencePanel(team));return section;}
async function load(week){throughWeek=Number(week);if(!Number.isInteger(throughWeek)||throughWeek<1){report=null;evidence=null;return null;}try{const response=await fetch(`/api/league/${window.DCC_LEAGUE_ID||'1389344338340761600'}/team-intelligence/${throughWeek}`,{cache:'no-store'}),data=await response.json();report=response.ok&&data.status==='verified-production'?data:null;try{const evidenceResponse=await fetch(`/api/league/${window.DCC_LEAGUE_ID||'1389344338340761600'}/team-component-matrix/${throughWeek}`,{cache:'no-store'});const candidate=await evidenceResponse.json();evidence=evidenceResponse.ok&&candidate.status==='ready'&&candidate.strengthAxes?.status==='ready'&&candidate.strengthAxes?.publication?.overallRankingPublished===false?candidate:null;}catch{evidence=null;}return report;}catch{report=null;evidence=null;return null;}}
window.DCCTeamIntelligence={load,panel};})();
