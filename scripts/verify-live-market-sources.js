import assert from'node:assert/strict';import{fetchLiveMarketSnapshots}from'../lib/providers/live-market-sources.js';
const responses=new Map([['api.fantasycalc.com',new Response(JSON.stringify([{player:{id:1,sleeperId:'q1',name:'Q One',position:'QB',team:'NE'},value:5000}]),{status:200})],['api.statsguyfantasy.com',new Response('nope',{status:503})],['raw.githubusercontent.com',new Response('player,pos,team,value_1qb,value_2qb,scrape_date,fp_id\nQ One,QB,NE,4000,6000,2026-09-18,dp1',{status:200})]]);
const fetchImpl=async url=>{for(const[k,r]of responses)if(String(url).includes(k))return r.clone();return new Response('',{status:404});};
const out=await fetchLiveMarketSnapshots({fetchImpl});assert.equal(out.status,'ready');assert.equal(out.sourceCount,2);assert.deepEqual(out.snapshots.map(x=>x.source),['fantasycalc','dynastyprocess']);assert.equal(out.failures[0].source,'statsguy');assert.equal(out.provenance.failedSourcesNeverFabricated,true);console.log(JSON.stringify(out,null,2));
