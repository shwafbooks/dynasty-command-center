import assert from'node:assert/strict';import{runFormulaLab}from'../lib/formula-lab.js';
const matrix={status:'ready',teams:[{rosterId:'1',team:'Stars',verifiedProduction:200,lineupMarketCore:.95,marketDepth:.3,replacementAdvantage:.4},{rosterId:'2',team:'Deep',verifiedProduction:190,lineupMarketCore:.7,marketDepth:.9,replacementAdvantage:.3},{rosterId:'3',team:'Hot',verifiedProduction:260,lineupMarketCore:.6,marketDepth:.4,replacementAdvantage:.1}]};
const r=runFormulaLab({matrix,candidates:[{name:'balanced',weights:{verifiedProduction:1,lineupMarketCore:1,marketDepth:1,replacementAdvantage:1}},{name:'core-emphasis',weights:{verifiedProduction:1,lineupMarketCore:3,marketDepth:.5,replacementAdvantage:1}},{name:'production-emphasis',weights:{verifiedProduction:4,lineupMarketCore:1,marketDepth:.5,replacementAdvantage:.5}}]});assert.equal(r.status,'ready');assert.equal(r.published,false);assert.equal(r.provenance.aiSelectedWeights,false);assert.equal(r.provenance.publishedRanking,false);assert.equal(r.results.length,3);assert.notDeepEqual(r.results[1].teams.map(t=>t.rosterId),r.results[2].teams.map(t=>t.rosterId));for(const candidate of r.results)assert.equal(candidate.status,'diagnostic-only');
console.log(JSON.stringify({published:r.published,candidates:r.results.map(x=>({name:x.name,weights:x.weights,diagnosticOrder:x.teams.map(t=>t.team)}))},null,2));
