import assert from'node:assert/strict';import{buildEvidenceClaim,buildNarrativeBrief}from'../lib/evidence-claim.js';
const gate={status:'supported',allowed:true,claimType:'production-observation',evidence:[{metricId:'wr-points',label:'WR Verified Points',value:87.2,classification:'derived-metric',formulaVersion:'team-intelligence-production-v1.1',confidence:{confidence:.94,label:'very-high'},sources:[{source:'Sleeper+DCC verified scoring'}]}],provenance:{gateVersion:'conclusion-gate-v1'}};
const claim=buildEvidenceClaim({gate,subject:{team:'A'},headlineFact:'A has 87.2 verified WR points.',comparison:{leagueRank:2,of:10},context:['through Week 1']});assert.equal(claim.status,'ready');assert.equal(claim.narrativeAllowed,true);assert.equal(claim.constraints.mayChangeMetricValues,false);assert.equal(claim.constraints.mayInferManagerIntent,false);assert.equal(claim.constraints.mayDeclareTradeWinner,false);
const brief=buildNarrativeBrief(claim);assert.equal(brief.status,'ready');assert.equal(brief.evidence[0].value,87.2);assert.ok(brief.writerInstructions.some(x=>x.includes('do not invent facts')));
const blocked=buildEvidenceClaim({gate:{status:'withheld',allowed:false,reasons:['missing-publishable-metric:wr-points']},subject:{team:'B'}});assert.equal(blocked.status,'withheld');assert.equal(blocked.narrativeAllowed,false);assert.equal(buildNarrativeBrief(blocked).status,'withheld');
console.log(JSON.stringify({claim:{status:claim.status,constraints:claim.constraints,evidence:claim.evidence},brief:{status:brief.status,instructions:brief.writerInstructions},blocked},null,2));
