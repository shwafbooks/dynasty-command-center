// DCC Sleeper League Format Adapter
// Converts Sleeper roster_positions into the generic lineup-capacity input without hard-coding a league.
export const SLEEPER_LEAGUE_FORMAT_VERSION='sleeper-league-format-v1';
const BENCH=new Set(['BN','BENCH','IR','TAXI']);
const MAP={QB:'QB',RB:'RB',WR:'WR',TE:'TE',FLEX:'FLEX',WRT:'FLEX',REC_FLEX:'FLEX',SUPER_FLEX:'SUPERFLEX',SUPERFLEX:'SUPERFLEX',OP:'SUPERFLEX'};
export function deriveSleeperLineupSettings(rosterPositions=[]){const settings={QB:0,RB:0,WR:0,TE:0,FLEX:0,SUPERFLEX:0},unsupported={},bench={BN:0,IR:0,TAXI:0};for(const raw of rosterPositions||[]){const slot=String(raw||'').trim().toUpperCase();if(!slot)continue;if(BENCH.has(slot)){const key=slot==='BENCH'?'BN':slot;bench[key]=(bench[key]||0)+1;continue;}const mapped=MAP[slot];if(mapped)settings[mapped]++;else unsupported[slot]=(unsupported[slot]||0)+1;}const supportedStartingSlots=Object.values(settings).reduce((a,b)=>a+b,0);const unsupportedStartingSlots=Object.values(unsupported).reduce((a,b)=>a+b,0);return{status:unsupportedStartingSlots?'partial':'ready',settings,bench,unsupported,supportedStartingSlots,unsupportedStartingSlots,totalRosterSlots:(rosterPositions||[]).length,provenance:{formulaVersion:SLEEPER_LEAGUE_FORMAT_VERSION,source:'Sleeper league.roster_positions',hardCodedLeagueFormat:false,aiAdjusted:false},interpretation:{caution:unsupportedStartingSlots?'Unsupported starting slot types are disclosed and must be implemented before format-dependent intelligence is published.':'All supplied starting slot types are supported by this adapter.'}};}
