// Summarizes replacement-aware roster evidence without creating a strength score or team ranking.
export const TEAM_REPLACEMENT_PROFILE_VERSION='team-replacement-profile-v1';const POSITIONS=['QB','RB','WR','TE'];const round=(v,n=4)=>{const p=10**n;return Math.round((Number(v||0)+Number.EPSILON)*p)/p;};const mean=a=>a.length?round(a.reduce((x,y)=>x+y,0)/a.length):null;
export function buildTeamReplacementProfiles(evidence={}){if(evidence?.status!=='ready')return{status:'withheld',teams:[],teamRankingPublished:false,strengthScorePublished:false};const teams=(evidence.teams||[]).map(t=>{const positions={};for(const pos of POSITIONS){const rows=t.positions?.[pos]?.players||[],advantages=rows.map(p=>Number(p.marketAdvantageVsBestAvailable)).filter(Number.isFinite).sort((a,b)=>b-a),above=advantages.filter(x=>x>0);positions[pos]={valuedRosterPlayers:advantages.length,playersAboveBestAvailable:above.length,shareAboveBestAvailable:advantages.length?round(above.length/advantages.length):null,meanAdvantage:mean(advantages),top3MeanAdvantage:mean(advantages.slice(0,3)),bestAdvantage:advantages.length?advantages[0]:null,replacementBaseline:t.positions?.[pos]?.replacement?.bestAvailable?.consensusPercentile??null};}return{rosterId:t.rosterId,team:t.team,manager:t.manager,positions};});return{status:'ready',teams,teamRankingPublished:false,strengthScorePublished:false,interpretation:{meaning:'Descriptive summary of how many market-valued roster assets sit above the best currently available same-position alternative.',caution:'No cross-position weights are applied. This does not rank teams or declare roster strength.'},provenance:{formulaVersion:TEAM_REPLACEMENT_PROFILE_VERSION,arbitraryWeights:false,aiAdjusted:false}};}